import { Nullable } from 'tsdef';
export declare enum ChonkyView {
    Details = "details",
    SmallThumbs = "small_thumbs"
}
export interface FileViewConfig {
    entryWidth?: Nullable<number>;
    entryHeight: number;
}
export interface FileEntrySize {
    width?: Nullable<number>;
    height: number;
}
